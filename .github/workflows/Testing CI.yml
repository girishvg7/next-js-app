name: Test Branch Workflow

on:
  push:
    branches:
      - test   # This ensures it only runs on the 'test' branch
  pull_request:
    branches:
      - test

jobs:
  run-on-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "👋 Hello from the test branch workflow!"

      - name: Simulate a test command
        run: |
          echo "✅ Running tests..."
          echo "Tests passed!"
